interface:
  display_name: "Meeting Report"
  short_description: "Create compact, verified research meeting briefs"
  default_prompt: "Use $meeting-report to create a verified, bullet-first research meeting brief; publish it to Notion only when I explicitly request that."
